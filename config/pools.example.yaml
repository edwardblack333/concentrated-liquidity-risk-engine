version: 1
default_pool_id: eth_usdc_005

pools:
  - pool_id: eth_usdc_005
    display_name: Uniswap v3 ETH/USDC 0.05%
    chain: ethereum
    protocol: uniswap_v3
    pool_address: 0x88e6A0c2dDD26FEEb64F039a2c41296FcB3f5640
    token0: USDC
    token1: WETH
    fee_tier_bps: 5
    quote_asset: USDC
    price_source:
      type: binance
      symbol: ETHUSDT
    bands:
      - 0.05
      - 0.10
      - 0.20
    week_anchor: monday
    active: true
    current_status:
      production_ready: false
      has_price_data: false
      has_fee_data: false
      has_liquidity_data: false
      has_regime_calibration: false
      has_deribit_overlay: false

    # Future pool-level fields can be introduced here without changing
    # current ETH/USDC 0.05% production model logic.
    data_paths: {}
    output_paths: {}
    calibration: {}
    overlays: {}

# Public example only. Private data paths, paid exports, API credentials, and live output paths are intentionally omitted.

